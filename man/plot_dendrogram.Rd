% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dendrogram.r
\name{plot_dendrogram}
\alias{plot_dendrogram}
\title{Plot a dendrogram as ggplot with optional tip aesthetics (color/fill/shape/size) and labels}
\usage{
plot_dendrogram(
  dend,
  metadata = NULL,
  label_from = NULL,
  color_by = NULL,
  color_palette = NULL,
  fill_by = NULL,
  fill_palette = NULL,
  shape_by = NULL,
  shape_palette = NULL,
  size_by = NULL,
  size_palette = NULL,
  tip_meta = NULL,
  label_col = "label",
  point_size = 2,
  point_stroke = NULL,
  default_shape = 21,
  orientation = c("top", "right", "bottom", "left"),
  first_branch = c("bottom", "top"),
  add_tip_labels = FALSE,
  tip_label_size = 3,
  leaf_order = NULL,
  rotate_to_leaf_order = TRUE,
  theme_obj = ggplot2::theme_void(),
  guides_obj = NULL,
  legend_position = "right",
  legend_position_inside = NULL
)
}
\arguments{
\item{dend}{A \code{dendrogram} object, or a list from \code{\link{build_dendrogram}} containing \code{$dendrogram}.}

\item{metadata}{Optional data frame used to build \code{tip_meta} automatically (recommended).}

\item{label_from}{Column in \code{metadata} that matches the dendrogram leaf labels.}

\item{color_by}{Optional column mapped to point outline color.}

\item{color_palette}{Optional named vector for \code{color_by} levels.}

\item{fill_by}{Optional column mapped to point interior fill.}

\item{fill_palette}{Optional named vector for \code{fill_by} levels.}

\item{shape_by}{Optional column mapped to point shape.}

\item{shape_palette}{Optional values passed to \code{ggplot2::scale_shape_manual()}.}

\item{size_by}{Optional column mapped to point size.}

\item{size_palette}{Optional named numeric vector passed to \code{ggplot2::scale_size_manual()}.}

\item{tip_meta}{Optional precomputed tip metadata. Must contain \code{label_col} and any requested mapping columns.}

\item{label_col}{Name of the label column inside \code{tip_meta} (default: \code{"label"}).}

\item{point_size}{Numeric tip point size (default: 2). Ignored when \code{size_by} is used.}

\item{point_stroke}{Numeric outline width for points (\code{stroke}); only applies to shapes 21--25.
If \code{NULL}, uses ggplot2 default.}

\item{default_shape}{Integer shape used when \code{fill_by} is set but \code{shape_by} is \code{NULL}
(default: 21). This ensures mapped fills are visible.}

\item{orientation}{Dendrogram orientation:
\describe{
\item{\code{"top"}}{Tips at bottom (default ggdendro layout).}
\item{\code{"bottom"}}{Tips at top (vertical mirror).}
\item{\code{"right"}}{Tips on left, branches extend to the right (rotated 90°).}
\item{\code{"left"}}{Tips on right, branches extend to the left (rotated 90° + mirrored).}
}}

\item{first_branch}{For \code{orientation="left"} or \code{"right"}, controls whether the first leaf
in the layout appears at the top (\code{"top"}) or bottom (\code{"bottom"}). This is implemented by
reversing the (post-flip) vertical ordering via \code{scale_x_reverse()}.
For \code{orientation="top"} or \code{"bottom"}, this parameter is currently ignored.}

\item{add_tip_labels}{Logical; if \code{TRUE}, draw tip labels.}

\item{tip_label_size}{Numeric text size for tip labels.}

\item{leaf_order}{Optional character vector giving the desired leaf order (matching dendrogram labels).}

\item{rotate_to_leaf_order}{Logical; if \code{TRUE} (default), rotate dendrogram to match \code{leaf_order}
using \code{dendextend::rotate()} (requires dendextend).}

\item{theme_obj}{ggplot2 theme to apply (default: \code{ggplot2::theme_void()}).}

\item{guides_obj}{Optional \code{ggplot2::guides(...)} added to the plot.}

\item{legend_position}{Legend position passed to \code{theme(legend.position=...)}.}

\item{legend_position_inside}{Numeric length-2 vector (x, y) between 0 and 1 used when \code{legend_position="inside"}.}
}
\value{
A \code{ggplot} object. Leaf order (left-to-right) is stored as \code{attr(plot, "leaf_order")}.
}
\description{
Converts a \code{dendrogram} into ggplot layers using \code{ggdendro}. Draws branches
as line segments and overlays tip (leaf) points. Tip aesthetics can be mapped to
outline color (\code{color}), interior fill (\code{fill}; visible for shapes 21-25),
shape (\code{shape}), and size (\code{size}).
}
\details{
Leaf order (left-to-right in the rendered dendrogram layout \emph{before orientation transforms})
is stored on the returned plot as \code{attr(plot, "leaf_order")}. This is useful to order
the alluvial axis to match the dendrogram.
}
