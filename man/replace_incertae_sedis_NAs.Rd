% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxonomy_cleaning.r
\name{replace_incertae_sedis_NAs}
\alias{replace_incertae_sedis_NAs}
\title{Normalize Incertae sedis and fill missing taxonomy ranks}
\usage{
replace_incertae_sedis_NAs(
  df,
  hierarchy = c("Domain", "Phylum", "Class", "Order", "Family", "Genus"),
  clean_levels = c("Phylum", "Class", "Order", "Family", "Genus")
)
}
\arguments{
\item{df}{A data.frame or tibble containing taxonomy columns.}

\item{hierarchy}{Character vector of column names in rank order from highest
(parent) to lowest (child). Only columns present in \code{df} are used.}

\item{clean_levels}{Character vector of ranks to normalize and fill. Only ranks
present in both \code{df} and \code{hierarchy} are used.}
}
\value{
An object of the same class as \code{df} (tibble in, tibble out) with cleaned
taxonomy columns.
}
\description{
Cleans hierarchical taxonomy columns by standardizing common "Incertae sedis"
variants, filling missing child ranks from stable parent ranks, and replacing
empty/uninformative entries with \code{"unknown"}.
}
\details{
The function is designed for rank-ordered taxonomy (e.g., Domain → Phylum → Class → ...),
and performs a downward pass through the hierarchy to propagate parent information.

The following rules are applied:
\itemize{
\item "Incertae sedis" variants (case-insensitive; underscores/spaces tolerated)
are converted to \code{"inc. sed."}.
\item Empty strings (\code{""}) in \code{clean_levels} are treated as missing (\code{NA}).
\item A parent is considered \emph{unstable} if it is missing (\code{NA}) or matches
\code{"unknown"}, \code{"unclassified"}, or \code{"inc. sed."} (case-insensitive).
\item If a child is \code{"inc. sed."} and the parent is stable, the child becomes
\code{"<parent> inc. sed."}; otherwise it becomes \code{"unknown"}.
\item If a child is missing and the parent is stable, the child becomes
\code{"<parent>, unclassified"}; otherwise it becomes \code{"unknown"}.
\item Underscores are converted to spaces and values are trimmed.
}
}
\examples{
tax_df <- data.frame(
  Domain = c("Bacteria", "Bacteria", NA),
  Phylum = c("Proteobacteria", "IncertaE_Sedis", "Actinobacteriota"),
  Class  = c(NA, "Alphaproteobacteria", ""),
  Order  = c("", "Rhizobiales", "inc. sed."),
  Family = c("Rhizobiaceae", NA, "unknown"),
  Genus  = c("", "inc. sed.", NA)
)

replace_incertae_sedis_NAs(
  df = tax_df,
  hierarchy = c("Domain", "Phylum", "Class", "Order", "Family", "Genus"),
  clean_levels = c("Phylum", "Class", "Order", "Family", "Genus")
)

}
